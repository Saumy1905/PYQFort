#!/bin/bash

# Back to project root (if not already)
cd ~/PYQFort-Rajveer

# Stage all changes
git add .

# Commit with a meaningful message
git commit -m "Added 16 PYQs for PPS (ESC-103) in 1st Sem - Computer Engineering"

# Push to the branch (assuming you're on 'SST' branch)
git push origin SST
